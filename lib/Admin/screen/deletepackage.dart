import 'package:flutter/material.dart';

class DeletePackage extends StatelessWidget {
  const DeletePackage({super.key});
static const String id = "deletepackage";
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("delete Package"),
    );
  }
}